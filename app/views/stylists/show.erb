<h2>Stylist: <%= @stylist.username %></h2>
<h3>Appointments:</h3>
<% @stylist.appointments.each do |appt| %>
    <ul>
        <li>When: <%= appt.appointment_datetime.strftime("%B %e, %Y at %l:%M %p") %></li>
        <li>Client: <%= appt.client.name %></li>
        <li>Service: <%= appt.service %></li>
    </ul>
    <% end %>

